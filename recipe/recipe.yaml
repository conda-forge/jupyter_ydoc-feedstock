# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "3.3.0"

package:
  name: jupyter_ydoc
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/j/jupyter_ydoc/jupyter_ydoc-${{ version }}.tar.gz
    sha256: ff5a12dff34c9c14bbdcedfa868c50e727d95953acaf7052d014e0a1b7dc5ecb
    target_directory: dist
  - url: https://github.com/jupyter-server/jupyter_ydoc/archive/refs/tags/v${{ version }}.tar.gz
    sha256: 882ae1a48eb571819f6d0b0196eae4c0582ca956e9aea81ab8db2cfa9c989d66
    target_directory: src

build:
  number: 0
  noarch: python
  script:
    - cd dist
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

requirements:
  host:
    - hatch-nodejs-version
    - hatchling >=1.10.0
    - pip
    - python ${{ python_min }}.*
  run:
    - python >=${{ python_min }}
    - pycrdt >=0.10.1,<0.13.0

tests:
  - python:
      imports: jupyter_ydoc
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - 3.14.*
  - files:
      recipe:
        - run_test.py
      source:
        - src/javascript/
        - src/lerna.json
        - src/package.json
        - src/tests/
        - src/yarn.lock
    requirements:
      run:
        - httpx-ws >=0.5.2
        - hypercorn >=0.16.0
        - nodejs >=22,<23
        - pycrdt-websocket >=0.16.0,<0.17.0
        - pytest-asyncio
        - pytest-cov
        - python ${{ python_min }}.*
        - yarn <4
    script:
      file: run_test.py
      interpreter: python

about:
  license: BSD-3-Clause
  license_file: dist/LICENSE
  summary: Document structures for collaborative editing using Ypy
  homepage: https://github.com/jupyter-server/jupyter_ydoc
  documentation: https://jupyter-ydoc.readthedocs.io

extra:
  recipe-maintainers:
    - davidbrochart
    - conda-forge/jupyter_server
